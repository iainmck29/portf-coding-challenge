import React from "react";
import { ResponsiveBar } from "@nivo/bar"

interface NivoPropType {
    data: string
}

const NivoResponsiveBar = ({data}: NivoPropType) => {
    return (
        <div></div>
        // <ResponsiveBar
        //     data={data}
        //     indexBy="beerSum"
        // />
)
}
